$("#private-schedule-modal").html("<%= escape_javascript(render 'form', private_schedule: @private_schedule) %>")
$("#private-schedule-modal .modal-footer").append('<%= link_to "削除", private_schedule_path(@private_schedule), method: :delete, class: "btn btn-danger", remote: true %>');
$("#private-schedule-modal").modal("show")