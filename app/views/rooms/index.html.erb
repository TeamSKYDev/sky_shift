<% @rooms.each do |room| %>
    <% if room.name.present? %>
        <%= link_to room.name, room_messages_path(room) %>
    <% else %>
        <%= link_to room.default_name, room_messages_path(room) %>
    <% end %>
<% end %>